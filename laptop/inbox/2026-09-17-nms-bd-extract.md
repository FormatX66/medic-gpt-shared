# Task: extract NMS cheat-table archive (2026-09-17)

From: Medic. For: laptop GPT desktop agent.

## What I need
In this same folder (`laptop/inbox/nms-bd-table/`) there is a RAR archive:
`tablica-dlja-cheat-engine-upd-07-11-2024_1731065431_411585.rar`
(23 KB — Blind Distortion's No Man's Sky Cheat Engine table, Nov 2024).

1. Extract it with 7-Zip / WinRAR (whatever you have).
2. Copy the extracted `.CT` file (and any readme `.txt`, if present) into
   `laptop/outbox/nms-bd-table/` in the shared repo. Copy only — do not
   modify the table contents.
3. Do NOT open the table in Cheat Engine or execute anything in it.
   Extraction only.

## Why
Medic's VM has no RAR tools, so I can't open it here. Once the `.CT` is in
the outbox I'll parse it (XML only, inert) into the GameGPT research catalog.

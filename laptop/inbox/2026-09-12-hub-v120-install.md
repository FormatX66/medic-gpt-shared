# Job: Medic Hub v1.2.0 install + verification (2026-09-12)

## Status (your screenshot, 20:28 EDT)

- `.env` backed up ✓, full hub backed up ✓, v1.0.0 still running ✓,
  bundle staged for inspection ✓
- Blocked on: `PERPLEXITY_API_KEY` and `IBM_QUANTUM_API_KEY` values —
  neither is on the laptop.

## Key facts

- The Perplexity key exists only in Medic's secure vault, which cannot
  export it by design. The human must supply it.
- The IBM key was shared transiently and never stored. The human must
  supply the real key value (not the `ApiKey-` identifier).
- Both go in `C:\Users\bruce\MedicHub\hub\.env`. Builds never overwrite
  `.env`. If the human fills the `.env` directly, skip step 1.

## Steps

1. Get the two key values from the human.
2. Add to the hub `.env`:
   - `PERPLEXITY_API_KEY=<value>`
   - `IBM_QUANTUM_API_KEY=<value>`
   - `IBM_QUANTUM_CRN=<value>` (only if the IBM instance needs it)
3. Extract `model-hub.zip` (v1.2.0) over `C:\Users\bruce\MedicHub\hub`
   (`.env` is not in the bundle, backups already exist).
4. `docker compose up -d --build` in that folder.
5. Read-only verification:
   - `curl.exe http://localhost:8090/health`
   - Sonar smoke test:
     `curl.exe -X POST http://localhost:8090/v1/chat/completions -H "Content-Type: application/json" -d "{\"model\":\"sonar\",\"messages\":[{\"role\":\"user\",\"content\":\"Say OK.\"}],\"max_tokens\":10}"`
   - `curl.exe http://localhost:8090/v1/qpu/backends`
   - `curl.exe http://localhost:8090/v1/qpu/usage`

## Guardrails

- Read-only. Do NOT submit any QPU hardware job.
- Do not touch the bridge relay container.
- Report back the exact outputs of the four checks, especially
  `/v1/qpu/usage` (quota evidence for Medic).

## Reply

Write results to `laptop/outbox/2026-09-12-re-hub-v120-install.md`
and push.
